<!DOCTYPE html>
<html>
  <head>
    <title>Dr.hair</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <div class="row">
        <!-- <header>
          <nav class="navbar navbar-fixed-top navbar-inverse ">
            <div class="container-fluid">
              <% if admin_signed_in? %>
                <span class="navbar-brand">Dr.hair</span>
                  <ul class="nav navbar-nav navbar-right">
                      <li class="nav-item"><%= link_to "Form", admin_forms_path %></li>
                      <li class="nav-item"><%= link_to "Length", admin_lengths_path %></li>
                      <li class="nav-item"><%= link_to "Style", admin_styles_path %></li>
                      <li class="nav-item"><%= link_to "Logout", destroy_admin_session_path, method: :delete %></li>
                  </ul>
              <% elsif user_signed_in? %>
                <span class="navbar-brand navbar_center">Dr.hair</span>
                  <ul class="nav navbar-nav navbar-right">
                    <form class="navbar-form navbar-left">
                      <div class="input-group">
                        <input type="text" class="form-control" placeholder="Search">
                        <div class="input-group-btn">
                          <button class="btn btn-default" type="submit">
                            <i class="glyphicon glyphicon-search"></i>
                          </button>
                        </div>
                      </div>
                    </form>
                    <li class="nav-item"><%= link_to "Logout", destroy_user_session_path %></li>
                  </ul>
              <% elsif request.fullpath == "/user/sign_in" %>
                <ul class="nav navbar-nav navbar-right">
                  <li><%= link_to "Log In", new_user_session_path %></li>
                </ul>
              <% else %>
                <ul class="nav navbar-nav navbar-right">
                  <span class="navbar-brand">Dr.hair</span>
                </ul>
              <% end %>
            </div>
          </nav>
        </header> -->
      </div>
      <div class="row">
        <main>
            <div class="col-xs-2">
            </div>
            <div class="col-xs-8">
              <%= yield %>
            </div>
            <div class="col-xs-2">
            </div>
        </main>
      </div>
    </div>
  </body>
</html>
